# Myanmar Sign Language Recognition — pinned to lu_msl_recognition_env
# Python 3.12.13
#
# Install (local GPU, CUDA 12.6 wheels — matches current env):
#   pip install -r requirements.txt

--extra-index-url https://download.pytorch.org/whl/cu126

# Core deep learning (for Nvidia GeForce GTX 1060)
torch==2.12.0+cu126
torchvision==0.27.0+cu126

# MediaPipe keypoint extraction (do not upgrade to >=0.10.30 for current setup)
mediapipe==0.10.18

# Computer vision (mediapipe 0.10.18 also pulls opencv-contrib-python==4.11.0.86)
opencv-python==4.11.0.86

# Scientific computing
numpy==1.26.4
scipy==1.17.1
scikit-learn==1.9.0

# Data handling
pandas==3.0.3
PyYAML==6.0.3

# Visualization
matplotlib==3.11.0
seaborn==0.13.2

# Progress bars
tqdm==4.68.2

# Experiment tracking
tensorboard==2.20.0
wandb==0.27.2

# Metrics
torchmetrics==1.9.0

# mediapipe 0.10.18 dependency pins (to avoid resolver drift)
protobuf==4.25.9
